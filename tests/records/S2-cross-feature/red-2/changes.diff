diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/App/Sources/ShelfApp.swift <run>/App/Sources/ShelfApp.swift
--- <fixture>/App/Sources/ShelfApp.swift	2026-09-17 18:58:33
+++ <run>/App/Sources/ShelfApp.swift	2026-09-17 19:50:56
@@ -1,6 +1,7 @@
 import Analytics
 import BookList
 import Catalog
+import Settings
 import SwiftUI
 
 /// The composition root. It is the only place that may import every layer, so it is where the
@@ -9,6 +10,7 @@
 struct ShelfApp: App {
     @State private var settings: CatalogSettings
     @State private var bookList: BookListViewModel
+    @State private var isSettingsPresented = false
 
     init() {
         let settings = CatalogSettings()
@@ -22,7 +24,12 @@
 
     var body: some Scene {
         WindowGroup {
-            BookListView(viewModel: bookList)
+            BookListView(viewModel: bookList) {
+                isSettingsPresented = true
+            }
+            .sheet(isPresented: $isSettingsPresented) {
+                SettingsView(settings: settings)
+            }
         }
     }
 }
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/fm-BookList/Sources/BookList/BookListView.swift <run>/Packages/fm-BookList/Sources/BookList/BookListView.swift
--- <fixture>/Packages/fm-BookList/Sources/BookList/BookListView.swift	2026-09-17 18:58:33
+++ <run>/Packages/fm-BookList/Sources/BookList/BookListView.swift	2026-09-17 19:50:49
@@ -6,9 +6,13 @@
 /// The shelf.
 public struct BookListView: View {
     private let viewModel: BookListViewModel
+    private let onSettingsTapped: () -> Void
 
-    public init(viewModel: BookListViewModel) {
+    /// `onSettingsTapped` is a closure, not a destination view, so this module never has to know
+    /// what settings look like — the composition root owns that and decides how to present it.
+    public init(viewModel: BookListViewModel, onSettingsTapped: @escaping () -> Void) {
         self.viewModel = viewModel
+        self.onSettingsTapped = onSettingsTapped
     }
 
     public var body: some View {
@@ -18,6 +22,13 @@
             }
             .listStyle(.plain)
             .navigationTitle("Shelf")
+            .toolbar {
+                ToolbarItem(placement: .topBarTrailing) {
+                    Button(action: onSettingsTapped) {
+                        Label("Settings", systemImage: "gearshape")
+                    }
+                }
+            }
         }
         .task {
             await viewModel.load()
