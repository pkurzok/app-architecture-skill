diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/bm-Analytics/Sources/Analytics/Analytics.swift <run>/Packages/bm-Analytics/Sources/Analytics/Analytics.swift
--- <fixture>/Packages/bm-Analytics/Sources/Analytics/Analytics.swift	2026-09-17 18:57:22
+++ <run>/Packages/bm-Analytics/Sources/Analytics/Analytics.swift	2026-09-17 19:33:07
@@ -15,4 +15,10 @@
             sink.track(event)
         }
     }
+
+    /// The one call every screen makes. A fixed event name and parameter key, so "which screen
+    /// appeared" reports the same shape everywhere, no matter how many screens exist.
+    public func trackScreenView(_ name: String) {
+        track(event: "screen_view", parameters: ["screen": name])
+    }
 }
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/bm-Analytics/Tests/AnalyticsTests/AnalyticsTests.swift <run>/Packages/bm-Analytics/Tests/AnalyticsTests/AnalyticsTests.swift
--- <fixture>/Packages/bm-Analytics/Tests/AnalyticsTests/AnalyticsTests.swift	2026-09-17 18:57:22
+++ <run>/Packages/bm-Analytics/Tests/AnalyticsTests/AnalyticsTests.swift	2026-09-17 19:33:39
@@ -21,4 +21,14 @@
     func trackingWithoutSinksIsHarmless() {
         Analytics().track(event: "book_list_loaded")
     }
+
+    @Test("A screen view reports its name under a stable event shape")
+    func screenViewReportsItsName() {
+        let sink = InMemoryAnalyticsSink()
+        let analytics = Analytics(sinks: [sink])
+
+        analytics.trackScreenView("BookList")
+
+        #expect(sink.events == [AnalyticsEvent(name: "screen_view", parameters: ["screen": "BookList"])])
+    }
 }
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/fm-BookList/Sources/BookList/BookListView.swift <run>/Packages/fm-BookList/Sources/BookList/BookListView.swift
--- <fixture>/Packages/fm-BookList/Sources/BookList/BookListView.swift	2026-09-17 18:58:33
+++ <run>/Packages/fm-BookList/Sources/BookList/BookListView.swift	2026-09-17 19:33:21
@@ -19,6 +19,7 @@
             .listStyle(.plain)
             .navigationTitle("Shelf")
         }
+        .trackScreenView("BookList", track: viewModel.trackScreenView)
         .task {
             await viewModel.load()
         }
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/fm-BookList/Sources/BookList/BookListViewModel.swift <run>/Packages/fm-BookList/Sources/BookList/BookListViewModel.swift
--- <fixture>/Packages/fm-BookList/Sources/BookList/BookListViewModel.swift	2026-09-17 18:58:33
+++ <run>/Packages/fm-BookList/Sources/BookList/BookListViewModel.swift	2026-09-17 19:33:18
@@ -20,6 +20,10 @@
         self.analytics = analytics
     }
 
+    /// What the view calls once, when the screen appears. Exposed as just this, not the whole
+    /// of `analytics`, so the view can't reach for anything wider than "report I appeared."
+    public var trackScreenView: (String) -> Void { analytics.trackScreenView }
+
     public func load() async {
         let loaded = (try? await store.load()) ?? []
         books = CatalogStore.sorted(loaded, by: settings.sortOrder)
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/fm-BookList/Tests/BookListTests/BookListViewModelTests.swift <run>/Packages/fm-BookList/Tests/BookListTests/BookListViewModelTests.swift
--- <fixture>/Packages/fm-BookList/Tests/BookListTests/BookListViewModelTests.swift	2026-09-17 19:00:07
+++ <run>/Packages/fm-BookList/Tests/BookListTests/BookListViewModelTests.swift	2026-09-17 19:33:47
@@ -20,4 +20,18 @@
 
         #expect(viewModel.books.map(\.title) == ["Zone One", "Dune", "Kindred"])
     }
+
+    @Test("trackScreenView forwards to analytics as a screen_view event")
+    func trackScreenViewForwardsToAnalytics() {
+        let sink = InMemoryAnalyticsSink()
+        let viewModel = BookListViewModel(
+            store: CatalogStore(),
+            settings: CatalogSettings(),
+            analytics: Analytics(sinks: [sink])
+        )
+
+        viewModel.trackScreenView("BookList")
+
+        #expect(sink.events == [AnalyticsEvent(name: "screen_view", parameters: ["screen": "BookList"])])
+    }
 }
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/fm-Settings/Package.swift <run>/Packages/fm-Settings/Package.swift
--- <fixture>/Packages/fm-Settings/Package.swift	2026-09-17 18:58:33
+++ <run>/Packages/fm-Settings/Package.swift	2026-09-17 19:33:27
@@ -6,11 +6,13 @@
     platforms: [.iOS(.v18)],
     products: [.library(name: "Settings", targets: ["Settings"])],
     dependencies: [
+        .package(path: "../bm-Analytics"),
         .package(path: "../bm-Catalog"),
         .package(path: "../lib-DesignSystem"),
     ],
     targets: [
         .target(name: "Settings", dependencies: [
+            .product(name: "Analytics", package: "bm-Analytics"),
             .product(name: "Catalog", package: "bm-Catalog"),
             .product(name: "DesignSystem", package: "lib-DesignSystem"),
         ]),
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/fm-Settings/Sources/Settings/SettingsView.swift <run>/Packages/fm-Settings/Sources/Settings/SettingsView.swift
--- <fixture>/Packages/fm-Settings/Sources/Settings/SettingsView.swift	2026-09-17 18:58:33
+++ <run>/Packages/fm-Settings/Sources/Settings/SettingsView.swift	2026-09-17 19:33:34
@@ -1,3 +1,4 @@
+import Analytics
 import Catalog
 import DesignSystem
 import SwiftUI
@@ -5,9 +6,11 @@
 /// The reader's preferences.
 public struct SettingsView: View {
     private let settings: CatalogSettings
+    private let analytics: Analytics
 
-    public init(settings: CatalogSettings) {
+    public init(settings: CatalogSettings, analytics: Analytics) {
         self.settings = settings
+        self.analytics = analytics
     }
 
     public var body: some View {
@@ -23,5 +26,6 @@
             .padding(.top, Spacing.small)
             .navigationTitle("Settings")
         }
+        .trackScreenView("Settings", track: analytics.trackScreenView)
     }
 }
diff -ruN -x .build -x .swiftpm -x *.xcodeproj -x DerivedData <fixture>/Packages/lib-DesignSystem/Sources/DesignSystem/ScreenViewTracking.swift <run>/Packages/lib-DesignSystem/Sources/DesignSystem/ScreenViewTracking.swift
--- <fixture>/Packages/lib-DesignSystem/Sources/DesignSystem/ScreenViewTracking.swift	1970-01-01 01:00:00
+++ <run>/Packages/lib-DesignSystem/Sources/DesignSystem/ScreenViewTracking.swift	2026-09-17 19:33:12
@@ -0,0 +1,9 @@
+import SwiftUI
+
+public extension View {
+    /// Reports that this screen appeared. Every screen adds this one modifier, passing its own
+    /// name and however it reports it — the view never needs to know what "reporting" means.
+    func trackScreenView(_ name: String, track: @escaping (String) -> Void) -> some View {
+        onAppear { track(name) }
+    }
+}
